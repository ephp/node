{% if chat is defined %}
    {# Aggiorna i messaggi di notifica #}
    notify_open = false;
    socketChat.on('updatenotify', function(user, n, data) {
        $('#notify_'+user.id).remove();
        $('#{{ notify_body }}').append(getNotifyBody(user, n, data));
    });
{% endif %}