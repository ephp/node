{% if chat is defined %}
    {# Aggiorna l'elenco delle chatroom #}
    socketChat.on('updateroomdetail', function(room) {
        console.log(room);
        /*
        $('#{{ room_body }}').html($('#{{ room_elem }}').html()
            .replace(/\[alias\]/g, alias)
            .replace(/\[id\]/g, room.id))
            .replace(/\[room\]/g, room_json))
            .replace(/\[user_id\]/g, user_id))
            .replace(/\[private\]/g, room.private ? 'private' : 'public'))
        ;
         */
    });
{% endif %}